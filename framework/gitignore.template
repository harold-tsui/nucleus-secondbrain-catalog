# ============================================================
#  SecondBrain 货架仓库 .gitignore（模板）
#  用法：复制到你的库根目录并改名为 .gitignore
#
#  策略：默认忽略一切，只放行「货架」元文件（目录骨架 + 导航）。
#  内容正文、二进制资产、内嵌 git 仓库、密钥一律不入库。
#  注意：本仓库用「显式 git add 货架文件」维护，不要用 `git add -A`，
#        以免误纳深层 README/yaml 或内嵌 git 仓库（gitlink）。
# ============================================================

# 1) 默认忽略所有文件，但允许 git 进入子目录以匹配白名单
*
!*/

# 2) 白名单：货架元文件（这些文件名只出现在领域骨架处）
!.gitignore
!STRUCTURE.md
!_guide.md
!_index.md
!naming.md

# 3) 根级文档与维护脚本（锚定到根，避免命中深层同名文件）
!/CLAUDE.md
!/README.md
!/bin/
!/bin/*.sh

# 4) 领域账本（逐个显式放行，避免误纳深层 yaml）
#    示例：!/10-family/members.yaml

# 5) 安全兜底：敏感文件即便误命中也排除
*.env
.env
.env.*
*secret*
*credential*
*.pem
*.key
*token*
.DS_Store
